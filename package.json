{
  "name": "demos",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://glukemh-h.github.io/Demos/",
  "dependencies": {
    "gh-pages": "^2.1.1",
    "mathjs": "^6.2.2",
    "react": "^16.10.2",
    "react-app-rewired": "^2.1.4",
    "react-dom": "^16.10.2",
    "react-scripts": "3.2.0"
  },
  "scripts": {
    "start": "react-app-rewired start",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "build": "react-app-rewired build",
    "test": "react-app-rewired test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
